
const initialState = {
    'selectedLanguageIndex': 0
}


const popularReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':
            return state;
        default:
            return state;
    }

}

export default popularReducer;